# 6.2 JetBrains IDE 集成

Claude Code 也可以在 JetBrains 系列 IDE 中使用，包括 IntelliJ IDEA、PyCharm、WebStorm 等。

---

## 支持的 IDE

| IDE | 语言/平台 |
|-----|----------|
| IntelliJ IDEA | Java, Kotlin |
| PyCharm | Python |
| WebStorm | JavaScript, TypeScript |
| GoLand | Go |
| PhpStorm | PHP |
| RubyMine | Ruby |
| CLion | C/C++ |
| Rider | .NET |

---

## 安装插件

### 方法 1：从插件市场安装

1. 打开 IDE
2. 进入 `Settings/Preferences` → `Plugins`
3. 选择 `Marketplace` 标签
4. 搜索 "Claude" 或 "Anthropic"
5. 点击 `Install`
6. 重启 IDE

### 方法 2：从文件安装

```bash
# 下载插件 ZIP 文件后
# Settings → Plugins → ⚙️ → Install Plugin from Disk
```

---

## 配置

### API 密钥配置

1. 进入 `Settings/Preferences` → `Tools` → `Claude`
2. 输入 API Key
3. 选择默认模型
4. 点击 `Apply`

```
┌─────────────────────────────────────────────┐
│ Settings                                     │
│ ─────────────────────────────────────────── │
│ Tools > Claude                               │
│                                             │
│ API Key: [sk-ant-xxxx...      ]             │
│ Model:   [Claude Sonnet 4    ▼]             │
│ Base URL: [                   ] (optional)  │
│                                             │
│ [Test Connection]  [Apply] [OK]             │
└─────────────────────────────────────────────┘
```

### 环境变量配置

也可以通过环境变量配置：

```bash
# ~/.bashrc or ~/.zshrc
export ANTHROPIC_API_KEY="sk-ant-xxxx"
```

---

## 功能使用

### 1. 工具窗口

Claude 工具窗口出现在 IDE 侧边栏：

```
┌─────────────────────────────────────────────┐
│ [Project] [Structure] [Claude]              │
│ ─────────────────────────────────────────── │
│ 🤖 Claude                                   │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ 输入你的问题...                          │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ [Send] [Clear]                              │
└─────────────────────────────────────────────┘
```

### 2. 右键菜单

在编辑器中右键访问 Claude 功能：

```
┌─────────────────────┐
│ Copy                │
│ Cut                 │
│ Paste               │
│ ─────────────────── │
│ Claude              │
│ ├── Explain Code    │
│ ├── Refactor        │
│ ├── Generate Tests  │
│ ├── Find Bugs       │
│ └── Ask Claude...   │
└─────────────────────┘
```

### 3. 意图动作（Intention Actions）

按 `Alt + Enter` 在代码上显示 Claude 建议：

```java
public void processData(List<String> data) {
    // 光标在这里，按 Alt + Enter
    // 显示：
    // 💡 Ask Claude to explain
    // 💡 Ask Claude to optimize
    // 💡 Ask Claude to add comments
}
```

---

## 快捷键

### 默认快捷键

| 快捷键 | 功能 |
|--------|------|
| `Alt + C` | 打开 Claude 工具窗口 |
| `Ctrl + Shift + C` | 发送选中代码到 Claude |
| `Alt + Enter` | 显示 Claude 建议 |

### 自定义快捷键

1. 进入 `Settings/Preferences` → `Keymap`
2. 搜索 "Claude"
3. 双击添加快捷键

```
┌─────────────────────────────────────────────┐
│ Keymap                                       │
│ ─────────────────────────────────────────── │
│ 🔍 Claude                                   │
│                                             │
│ Plugins > Claude                            │
│ ├── Open Claude Window     Alt+C            │
│ ├── Send to Claude         Ctrl+Shift+C     │
│ ├── Explain Code           (none)           │
│ └── Refactor with Claude   (none)           │
└─────────────────────────────────────────────┘
```

---

## 使用场景

### 1. 代码解释

```java
// 选中代码后右键 → Claude → Explain Code
public CompletableFuture<List<User>> fetchUsers() {
    return CompletableFuture.supplyAsync(() -> {
        return userRepository.findAll()
            .stream()
            .filter(User::isActive)
            .collect(Collectors.toList());
    });
}
```

### 2. 代码重构

```java
// 选中需要重构的代码
// 右键 → Claude → Refactor

// 输入重构目标：
// "将这个方法重构为使用 Stream API 的更简洁版本"
```

### 3. 生成测试

```java
// 打开一个类文件
// 右键 → Claude → Generate Tests

// Claude 会生成 JUnit 测试：
@Test
void testFetchUsers_returnsActiveUsers() {
    // ...
}
```

### 4. 代码审查

```java
// 在工具窗口中：
// "审查这个类的代码，找出潜在问题"
```

---

## 与 IDE 功能集成

### 与版本控制集成

```
# 在提交对话框中：
# 点击 "Generate Commit Message with Claude"

# Claude 分析 diff 并生成提交信息
```

### 与代码检查集成

Claude 可以作为额外的代码检查器：

```
Settings → Editor → Inspections → Claude
☑ Enable Claude Code Review
☑ Highlight potential issues
```

### 与重构工具集成

```
# 使用 IDE 重构功能时
# Claude 可以提供额外建议

Refactor → Claude Assisted Refactor
```

---

## 实验练习

### 实验 6.2.1：基本配置

```
1. 安装 Claude 插件
2. 配置 API Key
3. 点击 "Test Connection" 验证连接
4. 打开 Claude 工具窗口
```

### 实验 6.2.2：代码解释

```java
// 创建测试文件
public class Example {
    public static void main(String[] args) {
        List<Integer> numbers = Arrays.asList(1, 2, 3, 4, 5);
        int sum = numbers.stream()
            .filter(n -> n % 2 == 0)
            .mapToInt(Integer::intValue)
            .sum();
        System.out.println(sum);
    }
}

// 1. 选中代码
// 2. 右键 → Claude → Explain Code
// 3. 查看解释
```

### 实验 6.2.3：生成测试

```
1. 打开一个业务类
2. 右键 → Claude → Generate Tests
3. 查看生成的测试代码
4. 运行测试验证
```

---

## 故障排除

### 插件无法连接

```bash
# 检查网络
curl -I https://api.anthropic.com

# 检查代理设置
# Settings → Appearance & Behavior → System Settings → HTTP Proxy
```

### API 密钥无效

```
# 确认密钥格式正确
# 应以 sk-ant- 开头

# 检查是否有多余空格
# 重新输入密钥
```

### 响应缓慢

```
# 检查模型设置
# 使用 Haiku 模型会更快

# Settings → Tools → Claude → Model: Haiku
```

---

## 终端集成

在 JetBrains IDE 的终端中也可以直接使用 Claude Code CLI：

```bash
# 在 IDE 终端中
claude
> 分析当前项目的依赖关系
```

---

## 下一节

[6.3 其他编辑器](./03-other-editors.md) - 学习在 Vim、Emacs、Zed 等编辑器中使用 Claude Code
