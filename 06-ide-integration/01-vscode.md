# 6.1 VS Code 集成

VS Code 是 Claude Code 最成熟的 IDE 集成之一，提供丰富的功能。

---

## 安装扩展

### 方法 1：从扩展市场安装

1. 打开 VS Code
2. 按 `Cmd/Ctrl + Shift + X` 打开扩展面板
3. 搜索 "Claude Code"
4. 点击 "Install"

### 方法 2：命令行安装

```bash
code --install-extension anthropic.claude-code
```

### 方法 3：从 VSIX 安装

```bash
# 下载 VSIX 文件后
code --install-extension claude-code-x.x.x.vsix
```

---

## 功能概览

### 1. 侧边栏面板

扩展添加一个 Claude 图标到侧边栏：

```
┌─────────────────────────────────────┐
│ 📁 Explorer                         │
│ 🔍 Search                           │
│ 📦 Source Control                   │
│ 🤖 Claude Code  ← 点击打开         │
│ ...                                 │
└─────────────────────────────────────┘
```

功能：
- 开始新对话
- 查看对话历史
- 管理会话
- 查看使用量

### 2. 内联聊天

在编辑器中直接与 Claude 对话：

```
┌──────────────────────────────────────────────┐
│ function calculateTotal(items) {             │
│   // [Claude: 帮我优化这个函数]              │
│   let total = 0;                             │
│   for (let i = 0; i < items.length; i++) {   │
│     total += items[i].price;                 │
│   }                                          │
│   return total;                              │
│ }                                            │
└──────────────────────────────────────────────┘
```

快捷键：`Cmd/Ctrl + I`

### 3. 终端集成

在 VS Code 终端中使用 Claude Code：

```
┌──────────────────────────────────────────────┐
│ Terminal                                      │
│ ──────────────────────────────────────────── │
│ $ claude                                      │
│ > 帮我分析这个项目的结构                      │
│                                              │
│ Claude: 让我来分析项目结构...                 │
└──────────────────────────────────────────────┘
```

---

## 快捷键

### 默认快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd/Ctrl + I` | 打开内联聊天 |
| `Cmd/Ctrl + Shift + I` | 打开侧边栏面板 |
| `Cmd/Ctrl + L` | 发送选中代码到 Claude |
| `Escape` | 关闭内联聊天 |

### 自定义快捷键

在 `keybindings.json` 中添加：

```json
[
  {
    "key": "cmd+shift+c",
    "command": "claude-code.openChat"
  },
  {
    "key": "cmd+shift+e",
    "command": "claude-code.explainSelection"
  },
  {
    "key": "cmd+shift+r",
    "command": "claude-code.refactorSelection"
  }
]
```

---

## 设置配置

### 扩展设置

在 VS Code 设置（`Cmd/Ctrl + ,`）中搜索 "Claude"：

```json
// settings.json
{
  "claude-code.model": "claude-sonnet-4-20250514",
  "claude-code.maxTokens": 4096,
  "claude-code.autoSave": true,
  "claude-code.showInlineHints": true,
  "claude-code.terminal.enabled": true
}
```

### 常用设置

| 设置 | 说明 | 默认值 |
|------|------|--------|
| `claude-code.model` | 使用的模型 | sonnet |
| `claude-code.maxTokens` | 最大输出 token | 4096 |
| `claude-code.autoSave` | 自动保存修改 | true |
| `claude-code.showInlineHints` | 显示内联提示 | true |

---

## 使用场景

### 1. 代码解释

```
1. 选中代码
2. 右键 → "Ask Claude: Explain"
   或使用快捷键 Cmd/Ctrl + L
3. Claude 解释选中的代码
```

### 2. 代码重构

```
1. 选中需要重构的代码
2. 按 Cmd/Ctrl + I 打开内联聊天
3. 输入 "重构这段代码，提高可读性"
4. Claude 给出重构建议
5. 点击 "Accept" 应用更改
```

### 3. 生成测试

```
1. 打开要测试的文件
2. 在侧边栏输入 "为这个文件生成单元测试"
3. Claude 生成测试文件
4. 点击 "Create File" 创建测试文件
```

### 4. 调试帮助

```
1. 遇到错误时，选中错误信息
2. 右键 → "Ask Claude: Debug"
3. Claude 分析错误并提供解决方案
```

---

## 工作流集成

### 与 Git 集成

```
# 在侧边栏：
> 查看当前的 Git 更改并生成提交信息

# Claude 会：
# 1. 读取 git diff
# 2. 分析更改
# 3. 生成提交信息
```

### 与任务系统集成

创建 VS Code 任务（`.vscode/tasks.json`）：

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Claude: Review Code",
      "type": "shell",
      "command": "claude",
      "args": ["-p", "审查当前目录的代码"],
      "problemMatcher": []
    },
    {
      "label": "Claude: Generate Docs",
      "type": "shell",
      "command": "claude",
      "args": ["-p", "为 ${file} 生成文档"],
      "problemMatcher": []
    }
  ]
}
```

### 与代码片段集成

创建代码片段（`.vscode/snippets.code-snippets`）：

```json
{
  "Ask Claude": {
    "prefix": "claude",
    "body": [
      "// TODO: Ask Claude - $1"
    ],
    "description": "Add a Claude TODO comment"
  }
}
```

---

## 实验练习

### 实验 6.1.1：基本使用

```
1. 打开一个项目
2. 按 Cmd/Ctrl + Shift + I 打开 Claude 面板
3. 输入 "分析这个项目的结构"
4. 观察 Claude 的响应
```

### 实验 6.1.2：内联聊天

```
1. 打开一个代码文件
2. 选中一段代码
3. 按 Cmd/Ctrl + I 打开内联聊天
4. 输入 "解释这段代码"
5. 尝试 "重构这段代码"
```

### 实验 6.1.3：代码生成

```
1. 创建新文件
2. 打开侧边栏
3. 输入 "创建一个 React 组件，显示用户列表"
4. 应用生成的代码
```

---

## 故障排除

### 扩展无法激活

```bash
# 检查 Claude Code CLI 是否安装
claude --version

# 检查 API 密钥
echo $ANTHROPIC_API_KEY

# 重新加载 VS Code
# Cmd/Ctrl + Shift + P → "Developer: Reload Window"
```

### 内联聊天不工作

```json
// 检查设置
{
  "claude-code.inlineChat.enabled": true
}
```

### 连接问题

```bash
# 检查网络
curl https://api.anthropic.com/v1/messages -I

# 检查代理设置
echo $HTTP_PROXY
echo $HTTPS_PROXY
```

---

## 下一节

[6.2 JetBrains IDE 集成](./02-jetbrains.md) - 学习在 IntelliJ IDEA、PyCharm 等中使用 Claude
